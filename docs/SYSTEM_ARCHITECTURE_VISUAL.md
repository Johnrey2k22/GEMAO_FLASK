# GEMAO Flask - Visual System Overview

## 🏗️ Architecture Diagram

```
┌──────────────────────────────────────────────────────────────────┐
│                    GEMAO Flask Platform                          │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │         Web Browser (User Interface)                    │   │
│  │  ┌─────────┬──────────┬────────────┬──────────────┐    │   │
│  │  │ Login   │ Dashboard │ Games Play │ Leaderboard  │    │   │
│  │  └─────────┴──────────┴────────────┴──────────────┘    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │         Flask Application (app.py)                      │   │
│  │  ┌──────────────────────────────────────────────────┐  │   │
│  │  │ Router: / (Root)                                 │  │   │
│  │  │  ├─ Auth Blueprint (/auth)                       │  │   │
│  │  │  ├─ User Blueprint (/user)                       │  │   │
│  │  │  ├─ Admin Blueprint (/admin)                     │  │   │
│  │  │  ├─ Games Blueprint (/games)                     │  │   │
│  │  │  ├─ Leaderboard Blueprint (/leaderboard)         │  │   │
│  │  └──────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Core Services                                          │   │
│  │  ┌────────────┬──────────────┬────────────────────┐    │   │
│  │  │ Database   │ Authentication │ Game Engine       │    │   │
│  │  │ Connection │ System         │ (Pygame)          │    │   │
│  │  │            │                │                   │    │   │
│  │  │ db.py      │ utils.py       │ games/*.py        │    │   │
│  │  └────────────┴──────────────┴────────────────────┘    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │    External Services & Storage                          │   │
│  │  ┌──────────────┬────────────┬──────────────────┐      │   │
│  │  │ MySQL        │ Email      │ Game Files       │      │   │
│  │  │ Database     │ (SMTP)     │ (Pygame games)   │      │   │
│  │  │              │            │                  │      │   │
│  │  │ gemao_db     │ Gmail      │ naruto_run.py    │      │   │
│  │  │              │            │ etc.             │      │   │
│  │  └──────────────┴────────────┴──────────────────┘      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 📊 Blueprint Structure

```
Flask App (app.py)
│
├── Auth Blueprint (/auth) 🔐
│   ├── POST /login
│   ├── GET /logout
│   ├── GET/POST /register
│   ├── GET/POST /verify_otp
│   ├── GET /resend_otp
│   └── GET/POST /profile
│
├── User Blueprint (/user) 👤
│   ├── GET /dashboard
│   └── GET/POST /profile
│
├── Admin Blueprint (/admin) ⚙️
│   ├── GET /dashboard
│   ├── GET/POST /add_user
│   ├── POST /verify_add_user_otp
│   ├── GET/POST /add_game
│   ├── GET /users
│   ├── GET /games
│   ├── DELETE /users/<id>
│   └── DELETE /games/<id>
│
├──  Blueprint (/) 💰
│   └── GET /dashboard
│
├── Games Blueprint (/games) 🎮
│   ├── GET /
│   ├── GET/POST /play/<game_id>
│   └── POST /submit_score/<game_id>
│
├── Leaderboard Blueprint (/leaderboard) 🏆
│   ├── GET /
│   └── GET /game/<game_id>
│
└── Tournaments Blueprint (/tournaments) 🎯
    ├── GET /
    ├── GET/POST /create
    ├── GET/POST /<id>/join
    ├── GET /<id>
    └── POST /<id>/matches/<match_id>
```

---

## 🎮 Game System Architecture

```
GameBase (Abstract Base Class)
│
├── __init__()
├── @abstractmethod update()
├── @abstractmethod draw()
├── run() [Main game loop]
│
└── Inherited by 10 Games:
    ├── NarutoRun
    ├── ChakraCollector
    ├── JutsuBattle
    ├── RamenEater
    ├── NinjaMaze
    ├── SharinganPuzzle
    ├── SageModeTaining
    ├── AkatsukiHunt
    ├── VillageDefense
    └── BijuuCapture
```

---

## 💾 Database Schema

```
MySQL Database: gemao_db

┌─────────────────────────────────────────┐
│  users                                  │
├─────────────────────────────────────────┤
│ id, user_id, firstname, lastname        │
│ username*, password*, user_type         │
│ birthdate, address, mobile, email*      │
│ is_active                               │
└─────────────────────────────────────────┘
        ↓              ↓                  
┌──────────────┐  ┌─────────────────────┐
│user_profiles │  │ game_access         │
├──────────────┤  ├─────────────────────┤
│id, user_id   │  │id, user_id, game_id │
│profile_image │  │is_enabled           │
│personal_intro│  └─────────────────────┘
│dream_it_job  │       ↓
└──────────────┘  ┌─────────────────────┐
                  │ leaderboards        │
┌──────────────┐  ├─────────────────────┤
│games         │  │id, game_id*, user_id│
├──────────────┤  │score, created_at    │
│id, name      │  └─────────────────────┘
│description   │
│file_path     │  ┌──────────────────────────┐
│max_score     │  │ tournaments_tb            │
└──────────────┘  ├──────────────────────────┤
                  │id, name, game_id*,       │
┌──────────────────┤creator_id*, status      │
│otp_verification │└──────────────────────────┘
├──────────────────┤        ↓
│id, email*, otp   │┌──────────────────────────┐
│expires_at,       ││tournament_participants_tb│
│verified          │├──────────────────────────┤
└──────────────────┤│id, tournament_id*,       │
                   ││user_id*, joined_at      │
                   │└──────────────────────────┘
                   │        ↓
                   │┌──────────────────────────┐
                   ││matches_tb                │
                   │├──────────────────────────┤
                   ││id, tournament_id*,       │
                   ││round_num, player1_id,   │
                   ││player2_id, winner_id,   │
                   ││score1, score2, status   │
                   │└──────────────────────────┘
```

---

## 🔐 Authentication Flow

```
User
 │
 ├─► Register
 │    │
 │    └─► Email OTP Sent
 │         │
 │         └─► User Verifies OTP
 │              │
 │              └─► User Created in DB
 │                   │
 │                   └─► Can Login
 │
 └─► Login
      │
      ├─► Username & Password Check
      │    │
      │    ├─ Failed ✗ → Flash Error
      │    │
      │    └─ Success ✓
      │         │
      │         ├─► Password Hash Verified
      │         │    │
      │         │    └─► Session Created
      │         │         (user_id, user_name, user_role)
      │         │
      │         ├─► Role-Based Redirect
      │         │    ├─ admin → /admin/dashboard
      │         │    ├─  → //dashboard
      │         │    └─ user → /user/dashboard
      │         │
      │         └─► User Logged In ✓
      │
      └─► Logout
           │
           └─► Session Cleared
                │
                └─► Redirect to Login
```

---

## 📁 File Organization

```
GEMAO-FLASK/
│
├── 📄 Root Files
│   ├── app.py                          ← Entry Point
│   ├── requirements.txt
│   ├── .env.example
│   ├── .gitignore
│   └── README.md
│
├── 📂 MyFlaskapp/                      ← Main Application
│   │
│   ├── 📄 Core Files
│   │   ├── __init__.py                 ← Factory Pattern
│   │   ├── config.py                   ← Configuration
│   │   ├── db.py                       ← Database
│   │   ├── utils.py                    ← Utilities
│   │   └── models.py                   ← Base Classes
│   │
│   ├── 📁 auth/                        ← Authentication
│   │   ├── __init__.py
│   │   └── routes.py
│   │
│   ├── 📁 user/                        ← User Dashboard
│   │   ├── __init__.py
│   │   └── routes.py
│   │
│   ├── 📁 admin/                       ← Admin Panel
│   │   ├── __init__.py
│   │   └── routes.py
│   │
│   ├── 📁 /                     ←  Feature
│   │   ├── __init__.py
│   │   └── routes.py
│   │
│   ├── 📁 games/                       ← Games
│   │   ├── __init__.py
│   │   ├── routes.py
│   │   ├── game_base.py                ← Base Class
│   │   ├── naruto_run.py
│   │   ├── chakra_collector.py
│   │   ├── jutsu_battle.py
│   │   ├── ramen_eater.py
│   │   ├── ninja_maze.py
│   │   ├── sharingan_puzzle.py
│   │   ├── sage_mode_training.py
│   │   ├── akatsuki_hunt.py
│   │   ├── village_defense.py
│   │   └── bijuu_capture.py
│   │
│   ├── 📁 leaderboard/                 ← Leaderboards
│   │   ├── __init__.py
│   │   └── routes.py
│   │
│   ├── 📁 tournaments/                 ← Tournaments
│   │   ├── __init__.py
│   │   └── routes.py
│   │
│   ├── 📁 templates/
│   │   ├── base.html
│   │   ├── home.html
│   │   ├── 📁 auth/
│   │   ├── 📁 user/
│   │   ├── 📁 admin/
│   │   ├── 📁 /
│   │   ├── 📁 games/
│   │   ├── 📁 leaderboard/
│   │   └── 📁 tournaments/
│   │
│   └── 📁 static/
│       ├── favicon.ico
│       ├── 📁 images/
│       │   ├── naruto_bg.png
│       │   ├── naruto_character.png
│       │   ├── obstacle.png
│       │   ├── power_up.png
│       │   └── 📁 profiles/
│       └── 📁 sounds/
│           ├── background_music.mp3
│           ├── explosion.mp3
│           ├── fail.mp3
│           ├── jump.mp3
│           └── success.mp3
│
├── 📂 tests/                           ← Unit Tests
│   ├── test_models.py
│   ├── test_utils.py
│   └── test_naruto_run.py
│
└── 📂 .github/
    └── copilot-instructions.md         ← Architecture Guide
```

---

## 🎯 User Journey

```
Anonymous User
      │
      ├─► Visits http://localhost:5000
      │    │
      │    └─► Sees Home Page
      │         │
      │         ├─► Click "Login" → /auth/login
      │         │    │
      │         │    └─► Enter Credentials
      │         │         │
      │         │         └─► Redirect to Dashboard
      │         │
      │         └─► Click "Register" → /auth/register
      │              │
      │              ├─► Fill Registration Form
      │              │    │
      │              │    └─► Receive OTP Email
      │              │
      │              ├─► Verify OTP → /auth/verify_otp
      │              │    │
      │              │    └─► Account Created
      │              │
      │              └─► Login
      │
      │
Authenticated User
      │
      ├─► Dashboard (/user/dashboard)
      │    │
      │    ├─► View Games
      │    │    │
      │    │    └─► Play Game (/games/play/1)
      │    │         │
      │    │         ├─► Game Runs
      │    │         │    │
      │    │         │    └─► Score Submitted
      │    │         │
      │    │         └─► Back to Dashboard
      │    │
      │    ├─► View Leaderboards (/leaderboard/)
      │    │    │
      │    │    └─► See Rankings
      │    │
      │    ├─► Edit Profile (/user/profile)
      │    │    │
      │    │    └─► Update Information
      │    │
      │    └─► Logout
      │         │
      │         └─► Back to Home Page
      │
      │
Admin User
      │
      ├─► Admin Dashboard (/admin/dashboard)
      │    │
      │    ├─► Manage Users
      │    │    ├─► Add User
      │    │    ├─► View Users
      │    │    └─► Delete User
      │    │
      │    ├─► Manage Games
      │    │    ├─► Add Game
      │    │    ├─► View Games
      │    │    └─► Delete Game
      │    │
      │    └─► View Statistics
```

---

## 🔄 Request-Response Flow

```
┌──────────────┐
│ Browser Req  │
└──────┬───────┘
       │
       ▼
┌──────────────────────┐
│ Flask Route Handler  │
│ (routes.py)          │
└──────┬───────────────┘
       │
       ├─► Authentication Check
       │   (login_required decorator)
       │
       ├─► Authorization Check
       │   (admin_required decorator)
       │
       ├─► Input Validation
       │   (validate_email, etc.)
       │
       ├─► Database Query
       │   (get_db_connection)
       │
       ├─► Business Logic
       │
       ├─► Response Generation
       │   ├─ Render Template
       │   ├─ Return JSON
       │   └─ Redirect
       │
       └─► Flash Messages
           (Alert_Success/Alert_Fail)
           │
           ▼
     ┌──────────────────┐
     │ Browser Response │
     │ (HTML/JSON/Redir)│
     └──────────────────┘
```

---

## 📈 Deployment Architecture

```
Development:
┌─────────────┐
│ Python      │
│ app.py      │────► http://localhost:5000
│ (Debug)     │
└─────────────┘

Production:
┌──────────────────────────────────────┐
│ Gunicorn / Waitress WSGI Server      │
│ (Multiple Workers)                   │
└────────────────────┬─────────────────┘
                     │
                ┌────▼──────┐
                │ Nginx/    │
                │ Apache    │
                │ Reverse   │
                │ Proxy     │
                └────┬──────┘
                     │
                     │ HTTPS
                     ▼
            ┌─────────────────┐
            │ Client Requests │
            └─────────────────┘
```

---

## 🎊 System Features Summary

```
       ┌─────────────────────────────────────┐
       │   GEMAO Flask Gaming Platform       │
       └─────────────────────────────────────┘
                         │
         ┌───────────────┼────────────────┐
         │               │                │
    ┌────▼────┐   ┌─────▼──────┐  ┌──────▼────┐
    │ Games   │   │ Community  │  │ Admin     │
    │         │   │            │  │           │
    │ 10      │   │ Leaderboard│  │ User Mgmt │
    │ Pygame  │   │ Tournaments│  │ Game Mgmt │
    │ Games   │   │ Profiles   │  │ Stats     │
    └─────────┘   └────────────┘  └───────────┘
         │               │                │
         └───────────────┼────────────────┘
                         │
              ┌──────────▼──────────┐
              │ Authentication &    │
              │ Authorization       │
              │ (Login/Register)    │
              │ (OTP/Email)         │
              │ (Roles/Access)      │
              └─────────────────────┘
```

---

**This visualization shows the complete GEMAO Flask system architecture!**
